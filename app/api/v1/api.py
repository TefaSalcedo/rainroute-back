from fastapi import APIRouter

api_router = APIRouter()

# Future imports:
# from app.api.v1.endpoints import auth, users, routes
# api_router.include_router(auth.router, prefix="/auth", tags=["auth"])
